# Large data files are kept OUT of git entirely (see .gitignore), not in LFS.
# They are generated by scripts/build_metro_areas.py and hosted on Cloudflare
# R2 for production. Git LFS tracking was removed to stay within quota.
